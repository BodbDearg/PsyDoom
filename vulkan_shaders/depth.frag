#version 460

//----------------------------------------------------------------------------------------------------------------------
// Depth render shader: fragment.
// Used just to output depth to the depth buffer, for the purposes of masking sprites and geometry.
//----------------------------------------------------------------------------------------------------------------------
void main() {
    // This shader has no output, only the implicitly saved depth output...
}
